<UserControl x:Class="FluentFlyout.Controls.TaskbarWidgetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
             xmlns:local="clr-namespace:FluentFlyout.Controls"
             mc:Ignorable="d"
             Height="40"
             TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
             FontFamily="{Binding FontFamily}"
             FlowDirection="{Binding FlowDirection}">
    <Border Name="MainBorder" CornerRadius="6" MouseEnter="Grid_MouseEnter" MouseLeave="Grid_MouseLeave" MouseLeftButtonUp="Grid_MouseLeftButtonUp" ClipToBounds="True">
        <Grid>
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Opacity="0.4">
                <Image.Effect>
                    <BlurEffect Radius="80" />
                </Image.Effect>
            </Image>
            
            <Border Name="TopBorder" BorderThickness="0,1.25,0,0" CornerRadius="5" Margin="0,0,0,1.25">
                <Grid Name="MainGrid" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Grid.LayoutTransform>
                        <ScaleTransform ScaleX="0.9" ScaleY="0.9" />
                    </Grid.LayoutTransform>
                    <StackPanel Name="MainStackPanel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Width="Auto" Margin="4,0,4,0">
                        <Border Name="SongImageBorder" CornerRadius="5" Margin="0,0,0,-2" ClipToBounds="True" Width="36" Height="36" Panel.ZIndex="2">
                            <Grid>
                                <ui:SymbolIcon Name="SongImagePlaceholder" Symbol="MusicNote220" FontSize="24" Filled="True" Foreground="{DynamicResource MicaWPF.Brushes.SystemAccentColorTertiary}" />
                            </Grid>
                            <Border.Background>
                                <ImageBrush x:Name="SongImage" Stretch="UniformToFill" />
                            </Border.Background>
                        </Border>

                        <StackPanel Name="SongInfoStackPanel" Orientation="Vertical" VerticalAlignment="Center" Width="95" Margin="6,0,0,0" 
                                    ToolTipService.InitialShowDelay="800"
                                    ToolTipService.Placement="RelativePoint"
                                    ToolTipService.VerticalOffset="-16">
                            <local:MarqueeTextBlock x:Name="SongTitle" Text="" FontWeight="Normal" Height="18" Width="95" />
                            <local:MarqueeTextBlock x:Name="SongArtist" Text="" FontWeight="Normal" TextOpacity="0.5" Height="16" Width="95" Margin="0,-1.5,0,0" />
                        </StackPanel>

                        <StackPanel Name="ControlsStackPanel" Orientation="Horizontal" VerticalAlignment="Center" Width="Auto" Margin="4,0,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TaskbarWidgetControlsEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <ui:Button Name="PreviousButton" Appearance="Transparent" Icon="{ui:SymbolIcon Previous20, Filled=True}" Height="28" Width="28" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="Previous_Click"/>
                            <ui:Button Name="PlayPauseButton" Appearance="Transparent" Icon="{ui:SymbolIcon Pause16, Filled=True}" Height="28" Width="28" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="PlayPause_Click"/>
                            <ui:Button Name="NextButton" Appearance="Transparent" Icon="{ui:SymbolIcon Next20, Filled=True}" Height="28" Width="28" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="Next_Click"/>
                        </StackPanel>

                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
